// Tailwind CSS v4 via PostCSS. Vite (Astro's bundler) auto-loads this config
// and processes the `@import "tailwindcss"` in src/styles/global.css.
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
